#include <stdio.h>
/**
 * main - entryy point of function
 * Return: always (0) successful
 */
int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
			putchar('\n');
	return (0);
}
