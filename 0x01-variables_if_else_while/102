#include <stdio.h>
/**
 * main - entry point
 * Return: always (0) successful
 */
int main(void)
{
	int i;
	int j;

	for (:wq
			i = 0; i <= 98; i++)
	{
		for (j = i + 1; j <= 99; i++)
		{
			if (i == 98 && j == 99)
				break;
			{
				putchar(i + '0');
				putchar(' ');
				putchar(j + '0');
			}
		}
			if (!(i == 98 && j == 99))
				break;
			{
				putchar(',');
				putchar(' ');
			}
	}
	putchar('\n');
	return (0);
}
